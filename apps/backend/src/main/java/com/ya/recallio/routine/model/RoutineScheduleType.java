package com.ya.recallio.routine.model;

/**
 * Keeps routine cadence explicit so daily, weekly, and monthly planning stay easy to reason about.
 */
public enum RoutineScheduleType {
    DAILY,
    WEEKLY,
    MONTHLY
}
